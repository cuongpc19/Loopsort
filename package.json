{
  "name": "loopsort",
  "version": "0.1.0",
  "private": true,
  "description": "Candy Factory - a Loop Sort style puzzle: trays of candy boxes pour onto a conveyor belt",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -f",
    "check": "node factory-check.mjs",
    "headless": "node headless.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
